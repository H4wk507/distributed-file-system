\documentclass[12pt,a4paper]{article}
\usepackage[polish]{babel}
\usepackage[T1]{fontenc}
\usepackage[utf8x]{inputenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage[]{algorithm2e}
\usepackage{listings}

\usepackage{color}
\usepackage{listings}

\usepackage{tikz}
\usetikzlibrary{calc}
\usetikzlibrary{positioning,arrows.meta}
\usetikzlibrary{shapes.geometric}

\lstloadlanguages{
  Go,        % Dodanie Go
}

\setcounter{section}{0}

\definecolor{red}{rgb}{0.6,0,0} % for strings
\definecolor{blue}{rgb}{0,0,0.6}
\definecolor{green}{rgb}{0,0.8,0}
\definecolor{cyan}{rgb}{0.0,0.6,0.6}
\lstset{
    language=Go,
    basicstyle=\footnotesize\ttfamily,
    numbers=left,
    numberstyle=\tiny,
    numbersep=5pt,
    tabsize=2,
    extendedchars=true,
    breaklines=true,
    frame=b,
    stringstyle=\color{blue}\ttfamily,
    showspaces=false,
    showtabs=false,
    xleftmargin=17pt,
    framexleftmargin=17pt,
    framexrightmargin=5pt,
    framexbottommargin=4pt,
    commentstyle=\color{green},
    morecomment=[l]{//},  % dla komentarzy liniowych
    morecomment=[s]{/*}{*/},  % dla komentarzy wieloliniowych
    showstringspaces=false,
    morekeywords={
        package, import, func, var, const, type, go, defer, return,
        if, else, switch, case, fallthrough, for, range, break, continue,
        select, chan, interface, struct, map, gofmt, len, cap, append, copy,
        error, defer, panic, recover
    },
    keywordstyle=\color{cyan},  % kolory dla słów kluczowych
    identifierstyle=\color{red},  % kolory dla identyfikatorów
    commentstyle=\color{green},  % kolory dla komentarzy
    emph={main},  % Możesz też wyświetlić główną funkcję na inny sposób
    emphstyle=\bfseries\color{magenta}  % dla wyróżnionej funkcji "main"
}

\usepackage{caption}
\DeclareCaptionFont{white}{\color{white}}
\DeclareCaptionFormat{listing}{\colorbox{blue}{\parbox{\textwidth}{\hspace{15pt}#1#2#3}}}
\captionsetup[lstlisting]{format=listing,labelfont=white,textfont=white, singlelinecheck=false, margin=0pt, font={bf,footnotesize}}


\addtolength{\hoffset}{-1.5cm}
\addtolength{\marginparwidth}{-1.5cm}
\addtolength{\textwidth}{3cm}
\addtolength{\voffset}{-1cm}
\addtolength{\textheight}{2.5cm}
\setlength{\topmargin}{0cm}
\setlength{\headheight}{0cm}

\begin{document}
\begin{titlepage}
	\centering
	\vspace*{2cm}
	\begin{figure}[h]
	\centering
	\includegraphics[width=0.35\textwidth]{media/ps-logo.png}
	\end{figure}
	\vspace{1cm}
	{\LARGE\bfseries Modelowanie i analiza systemów informatycznych\par}
	\vspace{0.5cm}
	{\large dokumentacja projektu RSP - Rozproszony system plików\par}
	\vspace{2cm}
	{\Large Piotr Skowroński, Krzysztof Czuba, grupa 2\par}
	\vspace{0.5cm}
	{\large \today\par}
\end{titlepage}
\tableofcontents
\newpage
    \section{Część 1.}

\subsection{Definicja modelu systemu}
System został zaprojektowany jako \textbf{rozproszony system plików (DFS)} działający w architekturze hybrydowej, łączącej cechy modelu \textit{Master-Slave} (dla metadanych) oraz \textit{Peer-to-Peer} (dla wymiany danych).

Model systemu $S$ można zdefiniować jako krotkę:
$$ S = \langle N, M, F, \mathcal{L} \rangle $$
gdzie:
\begin{itemize}
    \item $N = \{n_1, n_2, ..., n_k\}$ -- zbiór węzłów przechowujących dane (Storage Nodes).
    \item $M$ -- wyróżniony węzeł koordynujący (Master), wybierany dynamicznie ze zbioru $N$ algorytmem elekcji.
    \item $F$ -- przestrzeń plików identyfikowana przez hash zawartości (Content-Addressed Storage).
    \item $\mathcal{L}$ -- logiczny czas Lamporta służący do porządkowania zdarzeń w systemie asynchronicznym.
\end{itemize}

Uzasadnieniem dla przyjętego modelu jest konieczność zapewnienia \textbf{wysokiej dostępności} (poprzez replikację i autorekonfigurację) oraz \textbf{spójności operacji} na współdzielonych zasobach (poprzez centralizację metadanych i mechanizm blokad).

\subsection{Opis programu}
Implementacja rozproszonego systemu plików, z uwzględnieniem problemu zakleszczeń i algorytmu elekcji.

System składa się z części serwerowej w której działa jeden węzeł główny (\texttt{master}) oraz wiele węzłów jedynie przechowujących dane (\texttt{storage}), oraz z web UI umożliwiającego download/upload plików.

\subsection{Wymagania wstępne i środowisko}

\subsubsection{Wymagania systemowe i narzędzia}
Do uruchomienia systemu wymagane są następujące narzędzia:
\begin{itemize}
    \item \textbf{System Operacyjny:} \textbf{Linux/MacOS} (testowane lokalnie).
    \item \textbf{Git} -- pobranie repozytorium.
    \item \textbf{Docker} (preferowana wersja 23+ dla wsparcia BuildKit) i \textbf{Docker Compose} -- uruchomienie backendu (API, baza danych, klaster RSP).
    \item \textbf{Node.js} (\texttt{22.x}) i \textbf{pnpm} (10.x) -- uruchomienie frontendu.
\end{itemize}

\subsubsection{Wymagania sieciowe}
Aby klaster działał poprawnie, należy zapewnić komunikację TCP:
\begin{itemize}
    \item Master powinien być osiągalny dla węzłów Storage na porcie \texttt{9000/TCP} (lub innym skonfigurowanym).
    \item Na maszynie zewnętrznej, na której działa węzeł Storage, port tego węzła (np. \texttt{9004/TCP}) musi zostać odblokowany w firewallu.
    \item Z Mastera powinno być możliwe zestawienie połączenia do \texttt{advertise-ip:port} węzła Storage.
\end{itemize}


\subsection{Instalacja i Wdrożenie}
Sekcja opisuje kroki niezbędne do uruchomienia systemu.

\begin{enumerate}
    \item Pobranie repozytorium:
\begin{verbatim}
git clone https://github.com/H4wk507/distributed-file-system
cd distributed-file-system
\end{verbatim}

    \item Uruchomienie backendu (API + baza danych + klaster RSP):
\begin{verbatim}
docker compose up --build
\end{verbatim}
\end{enumerate}

Backend uruchomi automatycznie PostgreSQL, wykona migracje oraz wystartuje klaster RSP (master + 3 węzły storage).

\subsection{Instrukcja Uruchomienia}

\subsubsection{Uruchomienie Backendu}
Backend uruchamiany jest poprzez Docker Compose:
\begin{verbatim}
docker compose up --build
\end{verbatim}

Komenda ta uruchomi:
\begin{itemize}
    \item PostgreSQL (baza danych),
    \item API HTTP (\texttt{http://localhost:8080}),
    \item Klaster RSP: 1 master + 3 węzły storage.
\end{itemize}

\subsubsection{Uruchomienie Frontendu}
Frontend uruchamiany jest osobno (wymaga Node.js 22.x i pnpm):
\begin{verbatim}
cd frontend
pnpm install
pnpm dev
\end{verbatim}

Po uruchomieniu interfejs WWW jest dostępny pod adresem \texttt{http://localhost:5173}.

	\subsection*{Dodatkowe informacje}

    Piotr Skowroński - Architektura systemu, praca nad testami, implementacja kluczowych algorytmów, backend, frontend.\\
    Krzysztof Czuba - backend, frontend, praca nad elementami przechowywania plików, dokumentacja.

	\newpage
	\section{Część II}
\subsection{Opis działania}
	Poniższa sekcja opisuje praktyczną realizację mechanizmów rozproszonych w systemie, koncentrując się na sposobie rozwiązania kluczowych problemów: synchronizacji czasu, wzajemnego wykluczania, detekcji zakleszczeń oraz odporności na awarie. Cała logika rozproszona została zaimplementowana w katalogu \texttt{backend/dfs/}.

	\subsubsection*{Architektura i komunikacja}
	System został podzielony na trzy logiczne warstwy, co zapewnia separację odpowiedzialności:
	
	\begin{enumerate}
		\item \textbf{Frontend} – interfejs użytkownika w technologii React. Komunikuje się wyłącznie z API.
		\item \textbf{API (Go)} – warstwa pośrednia odpowiadająca za autoryzację (JWT) i przechowywanie metadanych w relacyjnej bazie danych. API nie przechowuje zawartości plików, lecz deleguje operacje I/O do klastra za pośrednictwem \texttt{MasterClient}.
		\item \textbf{Klaster RSP} – warstwa składowania i logiki rozproszonej. Składa się z jednego węzła \textbf{Master} (koordynator) oraz wielu węzłów \textbf{Storage} (przechowywanie danych).
	\end{enumerate}

	\begin{figure}[htbp]
		\centering
		\begin{tikzpicture}[
			box/.style={draw, rounded corners, minimum width=2.2cm, minimum height=0.8cm, align=center, font=\small},
			db/.style={draw, cylinder, shape border rotate=90, aspect=0.3, minimum width=1.5cm, minimum height=1cm, font=\small},
			arrow/.style={-Latex, thick},
			node distance=1.2cm
		]
		% Frontend
		\node[box] (frontend) {Frontend\\(React)};
		
		% API layer
		\node[box, right=2cm of frontend] (api) {API\\(Go)};
		\node[db, below=0.8cm of api] (db) {PostgreSQL};
		
		% MasterClient
		\node[box, right=2cm of api] (client) {MasterClient};
		
		% RSP Cluster
		\node[box, right=2cm of client, fill=blue!10] (master) {Master};
		\node[box, above right=0.5cm and 1.5cm of master, fill=green!10] (s1) {Storage 1};
		\node[box, right=1.5cm of master, fill=green!10] (s2) {Storage 2};
		\node[box, below right=0.5cm and 1.5cm of master, fill=green!10] (s3) {Storage 3};
		
		% Arrows
		\draw[arrow, <->] (frontend) -- node[above, font=\scriptsize]{HTTP} (api);
		\draw[arrow, <->] (api) -- (db);
		\draw[arrow, <->] (api) -- (client);
		\draw[arrow, <->] (client) -- node[above, font=\scriptsize]{TCP} (master);
		\draw[arrow, <->] (master) -- (s1);
		\draw[arrow, <->] (master) -- (s2);
		\draw[arrow, <->] (master) -- (s3);
		\draw[arrow, <->, dashed] (s1) -- (s2);
		\draw[arrow, <->, dashed] (s2) -- (s3);
		
		% Labels
		\node[below=0.3cm of frontend, font=\scriptsize] {port 5173};
		\node[below=0.3cm of api, font=\scriptsize] {};
		\node[below=0.3cm of master, font=\scriptsize] {port 9000};
		\end{tikzpicture}
		\caption{Architektura systemu RSP.}
	\end{figure}

	Węzły komunikują się dwoma kanałami:
	\begin{itemize}
		\item \textbf{JSON po TCP} – do wymiany komunikatów sterujących (elekcja lidera, żądania blokad, heartbeat).
		\item \textbf{Protokół binarny} – dedykowany do efektywnego transferu plików (uniknięcie narzutu base64/JSON).
	\end{itemize}

	Podczas uruchamiania nowy węzeł łączy się ze znanym adresem (seed node), wysyła komunikat powitalny (\texttt{MessageDiscovery}) i pobiera listę pozostałych węzłów w klastrze.

	\subsubsection{Model matematyczny systemu}
	Dla zapewnienia poprawności działania algorytmów rozproszonych, przyjęto następujące założenia formalne:
	
	\textbf{1. Spójność przyczynowa (Zegary Lamporta)}
	Niech $a$ i $b$ będą zdarzeniami w systemie, a $C(e)$ wartością zegara logicznego dla zdarzenia $e$. Relacja "happened-before" $\rightarrow$ implikuje:
	$$ a \rightarrow b \implies C(a) < C(b) $$
	W przypadku $C(a) = C(b)$, porządek ustala się deterministycznie na podstawie identyfikatorów węzłów: $ID(a) < ID(b)$.
	
	\textbf{2. Warunek bezpieczeństwa blokady (Safety)}
	Dla każdego zasobu $r$, w dowolnym momencie czasu $t$, liczba węzłów przebywających w sekcji krytycznej $CS$ nie może przekraczać 1:
	$$ \forall t: |\{ n \in N : state(n, r) = \text{IN\_CS} \}| \le 1 $$
	Gwarantowane jest to przez wymóg uzyskania potwierdzeń (ACK) od wszystkich działających węzłów ($|ACKs| = |N| - 1$) oraz warunek bycia na szczycie kolejki priorytetowej.
	
	\textbf{3. Replikacja danych}
	Dostępność pliku $f$ jest zachowana, dopóki liczba dostępnych replik $R_f$ spełnia warunek $R_f \ge 1$. System dąży do utrzymania stałego współczynnika replikacji $k=3$:
	$$ \lim_{t \to \infty} |replicas(f)| = k $$
	
	\subsubsection{Synchronizacja czasu (Zegary Lamporta)}
	W systemach rozproszonych brak wspólnego zegara fizycznego utrudnia ustalenie kolejności zdarzeń. Aby rozwiązać ten problem, zastosowaliśmy \textbf{Zegary Lamporta}.
	
	Mechanizm działa następująco: każdy węzeł utrzymuje lokalny licznik. Przy każdym zdarzeniu lokalnym lub wysłaniu wiadomości licznik jest inkrementowany. Przy odbiorze wiadomości węzeł aktualizuje swój zegar do wartości $max(lokalny, otrzymany) + 1$.
	
	Dzięki temu możemy określić relację "działo się przed" dla zdarzeń w systemie. W przypadku konfliktów (ta sama wartość zegara), jako kryterium rozstrzygające (tie-breaker) wykorzystujemy unikalne ID węzła. Pozwala to na deterministyczne sortowanie żądań dostępu do zasobów.

	\subsubsection{Wzajemne wykluczanie (Rozproszone blokady)}
	Dostęp do współdzielonych zasobów (plików) musi być synchronizowany, aby uniknąć niespójności. Zaimplementowaliśmy algorytm oparty na kolejkach żądań i wymianie komunikatów.
	
	Procedura uzyskania blokady:
	\begin{enumerate}
		\item Węzeł rozsyła komunikat \texttt{LockRequest} do wszystkich pozostałych węzłów.
		\item Każdy węzeł (w tym wnioskujący) dodaje żądanie do swojej lokalnej kolejki, sortowanej według czasu Lamporta.
		\item Odbiorcy odsyłają potwierdzenie (\texttt{LockAck}).
		\item Wnioskujący wchodzi do sekcji krytycznej tylko wtedy, gdy:
		\begin{itemize}
			\item otrzymał potwierdzenia od \textbf{wszystkich} węzłów,
			\item jego żądanie znajduje się na \textbf{szczycie} kolejki (ma najniższy znacznik czasu).
		\end{itemize}
		\item Po zakończeniu operacji węzeł zwalnia blokadę, a pozostali usuwają jego żądanie z kolejki.
	\end{enumerate}
	Wymóg uzyskania potwierdzeń od całej grupy gwarantuje spójność widoku kolejki.

	\subsubsection{Wykrywanie zakleszczeń (Graf oczekiwań)}
	System blokad jest podatny na zakleszczenia (deadlock), np. gdy węzeł A czeka na zasób zajęty przez B, a B czeka na zasób zajęty przez A.
	
	Rolę arbitra pełni tutaj \textbf{Master}:
	\begin{itemize}
		\item Utrzymuje globalny \textbf{graf oczekiwań} (wait-for graph), odwzorowujący zależności między węzłami.
		\item Cyklicznie (co 10s) analizuje graf w poszukiwaniu cykli.
		\item Wykrycie cyklu oznacza zakleszczenie. Master wybiera węzeł o najniższym priorytecie w cyklu ("ofiarę") i wysyła mu polecenie \texttt{LockAbort}.
		\item Węzeł-ofiara musi zwolnić blokady, co przerywa cykl i umożliwia postęp pozostałym procesom.
	\end{itemize}

	\subsubsection{Awaria i rekonfiguracja (Algorytm Bully)}
	System musi być odporny na awarię koordynatora (Mastera). W takim przypadku pozostałe węzły muszą automatycznie wyłonić nowego lidera. Zastosowaliśmy algorytm \textbf{Bully}.
	
	\begin{enumerate}
		\item Węzły monitorują komunikaty \texttt{Heartbeat} od Mastera. Brak sygnału przez 15s jest traktowany jako awaria.
		\item Węzeł wykrywający awarię rozpoczyna elekcję, wysyłając zapytanie do wszystkich węzłów o wyższym identyfikatorze (priorytecie).
		\item Jeśli węzeł o wyższym priorytecie odpowie, przejmuje proces elekcji.
		\item Jeśli żaden węzeł o wyższym priorytecie nie odpowie, inicjator ogłasza się nowym Masterem.
	\end{enumerate}
	Po objęciu funkcji nowy Master przeprowadza synchronizację metadanych, zbierając raporty o posiadanych plikach od wszystkich węzłów Storage, co przywraca spójność systemu.

	\subsubsection{Dystrybucja danych (Consistent Hashing)}
	Aby zapewnić równomierne obciążenie i minimalizować konieczność przenoszenia danych przy zmianach w klastrze, zastosowaliśmy \textbf{Consistent Hashing}.
	
	\begin{itemize}
		\item Przestrzeń haszy traktowana jest jako pierścień.
		\item Każdy węzeł fizyczny jest mapowany na wiele punktów na pierścieniu (węzły wirtualne), co poprawia rozkład danych.
		\item Plik jest przypisywany do węzła, którego pozycja na pierścieniu jest pierwszą napotkaną zgodnie z ruchem wskazówek zegara od hasha pliku.
	\end{itemize}
	Dodatkowo system utrzymuje \textbf{współczynnik replikacji $r=3$}. Plik jest replikowany na trzy kolejne węzły fizyczne na pierścieniu, co zabezpiecza dane przed awarią pojedynczych maszyn.

	\subsubsection{Warstwa składowania (Storage)}
	Węzły przechowują pliki w systemie \textbf{Content-Addressed Storage}, gdzie identyfikatorem pliku jest hash jego zawartości (SHA256).
	\begin{itemize}
		\item Ścieżka pliku: \texttt{data/ab/abcd123...dat}
		\item Metadane: \texttt{data/ab/abcd123...metadata.json}
	\end{itemize}
	Podejście to zapewnia natywną \textbf{deduplikację} – identyczne pliki przesłane przez różnych użytkowników są przechowywane fizycznie tylko raz, co oszczędza przestrzeń dyskową.

	\subsubsection{Transfer plików (Streaming)}
	Ze względu na ograniczenia wydajnościowe formatu JSON przy przesyłaniu dużych porcji danych binarnych, zaimplementowaliśmy dedykowany protokół oparty na czystym TCP.
	
	Struktura pakietu przesyłanego w protokole strumieniowym (łącznie 64 bajty nagłówka):
	\begin{itemize}
		\item \textbf{Magic Byte} (1 bajt): Identyfikator operacji (\texttt{0x01} - Upload, \texttt{0x02} - Download).
		\item \textbf{Session ID} (36 bajtów): Unikalny identyfikator sesji (UUID) w postaci ciągu znaków.
		\item \textbf{File Size} (8 bajtów): Rozmiar przesyłanego pliku (int64, Big Endian).
		\item \textbf{Chunk Size} (4 bajty): Rozmiar pojedynczego fragmentu danych (int32, Big Endian).
		\item \textbf{Reserved} (15 bajtów): Pole rezerwowe (padding) dla wyrównania do 64 bajtów.
		\item \textbf{Data Stream}: Ciągły strumień bajtów pliku (payload) o długości \texttt{File Size}.
	\end{itemize}

	Proces przesyłania odbywa się z pominięciem Mastera: klient otrzymuje od Mastera listę adresów węzłów Storage, a następnie nawiązuje z nimi bezpośrednie połączenia równoległe. Eliminuje to wąskie gardło na węźle koordynującym.

	\newpage
	
	\subsubsection{Bezpieczeństwo i odporność na ataki}
	System implementuje mechanizmy zabezpieczające na kilku poziomach:
	
	\begin{enumerate}
		\item \textbf{Uwierzytelnianie i Autoryzacja:}
		\begin{itemize}
			\item Wykorzystanie standardu \textbf{JWT (JSON Web Token)} podpisywanego algorytmem HMAC-SHA256. Tokeny są wymagane przy każdym żądaniu do API.
			\item Hasła użytkowników są haszowane przy użyciu \textbf{bcrypt} z solą, co zabezpiecza przed atakami.
		\end{itemize}
		
		\item \textbf{Izolacja sieciowa:}
		\begin{itemize}
			\item Klaster RSP (Master/Storage) operuje na dedykowanych portach TCP. W środowisku produkcyjnym zalecana jest izolacja tych portów w prywatnej podsieci (VLAN), do której dostęp ma tylko API Gateway.
			\item Walidacja struktury pakietów binarnych chroni przed atakami typu \textit{buffer overflow} poprzez ścisłą kontrolę rozmiaru nagłówka (64 bajty) i payloadu.
		\end{itemize}
		
		\item \textbf{Odporność na ataki DoS (Denial of Service):}
		\begin{itemize}
			\item Architektura Master-Slave z bezpośrednim transferem danych (direct I/O) odciąża węzeł główny. Master obsługuje jedynie lekkie komunikaty sterujące, podczas gdy ciężki ruch sieciowy jest rozpraszany na węzły Storage.
			\item Limitowanie wielkości plików i timeouty na połączeniach TCP zapobiegają blokowaniu zasobów przez "zombie connections".
		\end{itemize}
	\end{enumerate}

	\subsection{Algorytmy}
Poniżej przedstawiono pseudokod kluczowych algorytmów zastosowanych w systemie.

	\subsubsection{Aktualizacja zegara Lamporta}
	\begin{algorithm}[H]
		\KwData{Lokalny zegar $L_i$, (opcjonalnie) odebrany znacznik czasu $t$}
		\KwResult{Nowa wartość $L_i$}
		\eIf{zdarzenie lokalne lub wysłanie wiadomości}{
			$L_i \leftarrow L_i + 1$\;
		}{
			$L_i \leftarrow \max(L_i, t) + 1$\;
		}
		\caption{Zegar logiczny Lamporta.}
	\end{algorithm}

	\subsubsection{Rozproszona blokada (kolejka Lamporta + ACK)}
	\begin{algorithm}[H]
		\KwData{Zasób \texttt{resourceID}, zbiór znanych węzłów \texttt{peers}}
		\KwResult{Wejście do sekcji krytycznej lub oczekiwanie}
		\textbf{RequestLock(resourceID)}\;
		$L_i \leftarrow L_i+1$\;
		Utwórz \texttt{LockRequest(resourceID, nodeID=i, logicalTime=$L_i$)}\;
		Dodaj żądanie do \texttt{lockQueue[resourceID]} i posortuj po $(logicalTime, nodeID)$\;
		Zainicjalizuj \texttt{pendingAcks[resourceID]}\;
		Rozgłoś \texttt{LockRequest} do wszystkich \texttt{peers}\;
		\BlankLine
		\textbf{OnReceiveLockRequest(req)}\;
		Dodaj \texttt{req} do lokalnej kolejki i posortuj\;
		Jeśli \texttt{lockHolders[resourceID]} istnieje i $\neq req.nodeID$, dodaj krawędź $req.nodeID \rightarrow holder$ do grafu oczekiwań\;
		Odeślij \texttt{LockAck(resourceID)} do nadawcy\;
		\BlankLine
		\textbf{CanEnterCriticalSection(resourceID)}\;
		\If{moje żądanie jest pierwsze w kolejce \textbf{i} liczba ACK $\geq |peers|$}{
			\Return \texttt{true}\;
		}
		\Return \texttt{false}\;
		\BlankLine
		\textbf{EnterCriticalSection(resourceID)}\;
		\If{\textbf{CanEnterCriticalSection(resourceID)} == \texttt{true}}{
			Rozgłoś \texttt{LockAcquired(resourceID)}\;
		}
		\caption{Rozproszona blokada zasobu.}
	\end{algorithm}

	\subsubsection{Wykrywanie i usuwanie zakleszczeń (graf oczekiwań)}
	\begin{algorithm}[H]
		\KwData{Graf oczekiwań $G=(V,E)$, priorytety węzłów}
		\KwResult{Przerwanie cyklu (jeśli wykryty)}
		\If{węzeł nie jest \texttt{master}}{\Return}
		\If{\textbf{HasCycle}$(G)$ zwraca \texttt{true} oraz ścieżkę cyklu $C$}{
			\texttt{victim} $\leftarrow \arg\min_{v \in C}\;priority(v)$\;
			Wyślij \texttt{LockAbort} do \texttt{victim}\;
		}
		\caption{Detekcja cyklu i wybór ofiary (master).}
	\end{algorithm}

	\subsubsection{Elekcja lidera Bully}
	\begin{algorithm}[H]
		\KwData{Priorytet własny $p_i$, lista \texttt{peers}}
		\KwResult{Ustalenie roli \texttt{master}}
		Wyślij \texttt{Election} do wszystkich węzłów o priorytecie $> p_i$\;
		\If{nie istnieje żaden węzeł o priorytecie $> p_i$}{
			Rozgłoś \texttt{Coordinator} i oczekuj krótko na ewentualny \texttt{Coordinator} od wyższego priorytetu\;
			Ustaw rolę \texttt{master}\;
			\Return
		}
		\If{otrzymano \texttt{OK} w czasie \texttt{electionTimeout}}{
			Zakończ elekcję (wyższy priorytet przejmie rolę)\;
			\Return
		}
		Rozgłoś \texttt{Coordinator} i ustaw rolę \texttt{master}\;
		\caption{Algorytm elekcji lidera Bully.}
	\end{algorithm}

	\subsubsection{Upload pliku (master + streaming do storage)}
	\begin{algorithm}[H]
		\KwData{\texttt{FileID}, \texttt{Filename}, \texttt{ContentType}, \texttt{Size}}
		\KwResult{Zapis pliku na $r=3$ storage nodes i aktualizacja metadanych}
		\textbf{Master:}\;
		Wybierz $r$ węzłów \texttt{storage} na podstawie consistent hashing (pierścień)\;
		Utwórz \texttt{sessionID} i prześlij do wybranych węzłów \texttt{StreamUploadReady(sessionID, FileID, ...)}\;
		Zwróć klientowi listę adresów strumieniowania (\texttt{ip:(port+100)})\;
		\BlankLine
		\textbf{Storage:}\;
		Po odebraniu strumienia zweryfikuj, czy \texttt{sessionID} istnieje\;
		Odczytaj dokładnie \texttt{Size} bajtów i zapisz w \texttt{LocalStorage}, licząc SHA256\;
		Wyślij do mastera \texttt{StreamStoreAck(sessionID, hash, nodeID)}\;
		\caption{Przepływ uploadu przez protokół streaming.}
	\end{algorithm}
	
	\subsubsection{Analiza złożoności}
	Poniżej przedstawiono złożoność obliczeniową i komunikacyjną kluczowych algorytmów dla systemu składającego się z $N$ węzłów.
	
	\begin{itemize}
		\item \textbf{Algorytm Elekcji Bully:}
		\begin{itemize}
			\item Złożoność komunikacyjna (najgorszy przypadek): $O(N^2)$ -- gdy awarii ulegnie lider, a elekcję rozpocznie węzeł o najniższym priorytecie.
			\item Złożoność komunikacyjna (najlepszy przypadek): $O(N)$ -- gdy elekcję rozpocznie węzeł o drugim najwyższym priorytecie.
		\end{itemize}
		
		\item \textbf{Rozproszona blokada (Lamport):}
		\begin{itemize}
			\item Liczba komunikatów na jedną sekcję krytyczną: $3 \times (N-1)$ (Request + Ack + Release). Złożoność $O(N)$.
			\item Opóźnienie wejścia: zależy liniowo od liczby węzłów w klastrze ze względu na konieczność zebrania potwierdzeń.
		\end{itemize}
		
		\item \textbf{Wykrywanie zakleszczeń (DFS):}
		\begin{itemize}
			\item Złożoność czasowa: $O(V + E)$, gdzie $V=N$ (węzły), a $E$ to liczba zależności (oczekiwań na locki). W najgorszym razie $E=N(N-1)$, co daje $O(N^2)$.
		\end{itemize}
		
		\item \textbf{Lookup pliku (Consistent Hashing):}
		\begin{itemize}
			\item Złożoność obliczeniowa: $O(\log K)$, gdzie $K$ to liczba wirtualnych węzłów na pierścieniu (wyszukiwanie binarne właściwego slotu). Operacja ta jest bardzo szybka i skalowalna.
		\end{itemize}
	\end{itemize}

	\subsection{Bazy danych}
	System wykorzystuje relacyjną bazę danych PostgreSQL do przechowywania informacji o użytkownikach oraz metadanych plików. Sama zawartość plików przechowywana jest w klastrze RSP (węzły \texttt{storage}), natomiast baza przechowuje jedynie metadane i stan replik.

	\subsubsection{Typy i tabele}
	Schemat jest definiowany w migracji \texttt{backend/migrations/000001\_init.up.sql}.
	\begin{itemize}
		\item \texttt{users} -- użytkownicy aplikacji:
		\begin{itemize}
			\item \texttt{id} (UUID, PK), \texttt{username} (UNIQUE), \texttt{email} (UNIQUE), \texttt{password\_hash}, \texttt{role} (ENUM \texttt{user\_role}), \texttt{created\_at}, \texttt{updated\_at}.
		\end{itemize}
		\item \texttt{files} -- metadane plików (nie dane binarne):
		\begin{itemize}
			\item \texttt{id} (UUID, PK), \texttt{name}, \texttt{size} (BIGINT), \texttt{hash}, \texttt{content\_type}, \texttt{owner\_id} (FK \textrightarrow{} \texttt{users.id}), \texttt{created\_at}, \texttt{updated\_at}.
		\end{itemize}
		\item \texttt{replicas} -- informacja o replikach danego pliku w klastrze RSP:
		\begin{itemize}
			\item \texttt{id} (UUID, PK), \texttt{file\_id} (FK \textrightarrow{} \texttt{files.id}), \texttt{node\_id} (UUID węzła RSP), \texttt{status} (ENUM \texttt{replica\_status}), \texttt{created\_at}, \texttt{updated\_at}.
			\item Constraint: \texttt{UNIQUE(file\_id, node\_id)} -- zapobiega duplikatom replik na tym samym węźle.
		\end{itemize}
	\end{itemize}

	Dodatkowo zdefiniowane są typy ENUM:
	\begin{itemize}
		\item \texttt{user\_role} \;\{\texttt{user}, \texttt{admin}\},
		\item \texttt{replica\_status} \;\{\texttt{synced}, \texttt{syncing}, \texttt{failed}\}.
	\end{itemize}

	W każdej tabeli pole \texttt{updated\_at} jest automatycznie aktualizowane przez trigger \texttt{update\_updated\_at\_column()}.

	\subsubsection{Relacje i integralność}
	\begin{itemize}
		\item Relacja \texttt{users (1) --- (N) files}: jeden użytkownik może posiadać wiele plików.
		\item Relacja \texttt{files (1) --- (N) replicas}: jeden plik może mieć wiele replik na węzłach RSP.
		\item Klucze obce mają \texttt{ON DELETE CASCADE} oraz \texttt{ON UPDATE CASCADE} (usunięcie użytkownika usuwa jego pliki; usunięcie pliku usuwa rekordy replik).
	\end{itemize}

	\subsubsection{Diagram ERD}
	\begin{figure}[htbp]
		\centering
		\begin{tikzpicture}[
			entity/.style={draw, rounded corners, align=left, font=\small, inner sep=6pt},
			crow/.style={thick}
		]
	\node[entity] (users) {
		\textbf{users}\\
		id (PK)\\
		username (UQ)\\
		email (UQ)\\
		password\_hash\\
		role: user\_role\\
		created\_at\\
		updated\_at
	};

	\node[entity, right=3.5cm of users] (files) {
		\textbf{files}\\
		id (PK)\\
		name\\
		size (BIGINT)\\
		hash\\
		content\_type\\
		owner\_id (FK)\\
		created\_at\\
		updated\_at
	};

	\node[entity, right=3.5cm of files] (replicas) {
		\textbf{replicas}\\
		id (PK)\\
		file\_id (FK)\\
		node\_id (UQ*)\\
		status: replica\_status\\
		created\_at\\
		updated\_at
	};

		% users -> files (1:N) crow's foot notation
		\draw[crow] (users.east) -- (files.west);
		\draw[crow] ($(users.east)+(0.15,0)$) -- ++(0,0.1) -- ++(0,-0.2); % "1" side (single line)
		\draw[crow] ($(files.west)+(-0.2,0)$) -- ++(-0.15,0.12); % crow foot
		\draw[crow] ($(files.west)+(-0.2,0)$) -- ++(-0.15,-0.12);
		\draw[crow] ($(files.west)+(-0.2,0)$) -- ++(-0.15,0);
		
		% files -> replicas (1:N) crow's foot notation
		\draw[crow] (files.east) -- (replicas.west);
		\draw[crow] ($(files.east)+(0.15,0)$) -- ++(0,0.1) -- ++(0,-0.2); % "1" side
		\draw[crow] ($(replicas.west)+(-0.2,0)$) -- ++(-0.15,0.12); % crow foot
		\draw[crow] ($(replicas.west)+(-0.2,0)$) -- ++(-0.15,-0.12);
		\draw[crow] ($(replicas.west)+(-0.2,0)$) -- ++(-0.15,0);

		\end{tikzpicture}
		\caption{Diagram ERD bazy danych. *UQ = część klucza unikalnego (file\_id, node\_id).}
	\end{figure}
    \newpage
	\subsection{Implementacja systemu}
	Sekcja opisuje implementację systemu z perspektywy podziału na moduły, ich odpowiedzialności oraz kluczowych przepływów (RSP i API/UI).

	\subsubsection{Struktura projektu}
	\begin{itemize}
		\item \texttt{backend/} -- implementacja RSP (węzły) oraz API (Go).
		\item \texttt{frontend/} -- interfejs WWW (React + Vite).
		\item \texttt{backend/migrations/} -- migracje bazy danych (PostgreSQL).
		\item \texttt{docker-compose.yml} -- uruchamianie środowiska i klastra.
	\end{itemize}

	\subsubsection{Backend API (Go)}
	Punkt wejścia API znajduje się w \texttt{backend/cmd/api/main.go}. Warstwa API jest zorganizowana typowo dla aplikacji usługowej:
	\begin{itemize}
		\item \texttt{backend/internal/handlers/} -- kontrolery HTTP (m.in. \texttt{auth\_handler.go}, \texttt{file\_handler.go}, \texttt{metrics\_handler.go}).
		\item \texttt{backend/internal/services/} -- logika domenowa (np. autoryzacja, obsługa plików).
		\item \texttt{backend/internal/database/} -- połączenie z PostgreSQL.
		\item \texttt{backend/internal/models/} -- modele danych: \texttt{User}, \texttt{File}, \texttt{Replica}.
		\item \texttt{backend/internal/middleware/} -- middleware (np. autoryzacja JWT).
	\end{itemize}
	API pełni rolę warstwy użytkowej: rejestracja/logowanie oraz operacje na plikach (upload/download/delete) delegowane do klastra RSP.

	\subsubsection{Klient Master (MasterClient)}
	Komunikacja między warstwą API a klastrem RSP odbywa się przez \texttt{MasterClient} (\texttt{backend/dfs/client/master\_client.go}). Jest to klient TCP, który:
	\begin{itemize}
		\item utrzymuje połączenie z aktualnym masterem klastra,
		\item automatycznie odkrywa nowego mastera po awarii (failover) poprzez odpytanie seed nodes,
		\item udostępnia metody do operacji na plikach:
		\begin{itemize}
			\item \texttt{InitStreamUpload} / \texttt{WriteChunk} / \texttt{Finalize} -- upload w trybie chunked (dla dużych plików),
			\item \texttt{UploadFileStream} -- upload całego pliku w jednym wywołaniu,
			\item \texttt{DownloadFileStream} -- download z weryfikacją hash SHA256,
			\item \texttt{DeleteFile} -- usunięcie pliku z klastra.
		\end{itemize}
	\end{itemize}
	
	Przepływ uploadu przez \texttt{MasterClient}:
	\begin{enumerate}
		\item API wywołuje \texttt{InitStreamUpload} -- master zwraca \texttt{sessionID} oraz listę adresów storage nodes.
		\item \texttt{MasterClient} otwiera połączenia TCP do wszystkich storage nodes i wysyła nagłówek binarny.
		\item API streamuje dane przez \texttt{WriteChunk} -- dane są równolegle wysyłane do wszystkich replik.
		\item \texttt{Finalize} czeka na potwierdzenia od storage nodes i zwraca hash pliku.
	\end{enumerate}

	\subsubsection{Węzły RSP (Go)}
	Punkt wejścia węzła RSP znajduje się w \texttt{backend/cmd/node/main.go}. Rdzeń stanowi struktura \texttt{Node} w \texttt{backend/dfs/node/node.go}, która uruchamia:
	\begin{itemize}
		\item serwer TCP do komunikacji sterującej (wiadomości JSON),
		\item serwer strumieniowania (port \texttt{port+100}) do transferu danych plików,
		\item pętle monitorujące: heartbeat, zdrowie peerów, time-outy blokad, detekcję zakleszczeń.
	\end{itemize}
	
	Kluczowe moduły:
	\begin{itemize}
		\item \textbf{Protokół wiadomości.} Typy wiadomości i struktury danych są w \texttt{backend/dfs/common/types.go} (m.in. \texttt{MessageWithTime}, \texttt{LockRequest}). Każda wiadomość przenosi znacznik czasu Lamporta, a odbiorca aktualizuje zegar regułą $L_i := \max(L_i, t)+1$.

		\item \textbf{Elekcja lidera.} Moduł \texttt{backend/dfs/election/bully.go} implementuje Bully; po wykryciu braku heartbeat lidera uruchamiana jest elekcja, a po wygranej ustawiana rola \texttt{master} oraz wykonywana synchronizacja metadanych.

		\item \textbf{Przechowywanie danych.} \texttt{backend/dfs/storage/local\_storage.go} zapisuje pliki adresowane zawartością (SHA256) oraz metadane w plikach JSON, utrzymując indeks \texttt{index.json}. Dzięki temu identyczna zawartość mapuje się na ten sam \texttt{hash}.
		
		\item \textbf{Rozmieszczenie i replikacja.} \texttt{backend/dfs/hashing/hash.go} realizuje consistent hashing z węzłami wirtualnymi (domyślnie 150). Master wybiera $r=3$ węzły storage jako repliki, a po elekcji wykonuje \texttt{MetadataRequest/Response} i naprawia brakujące repliki przez \texttt{ReplicateFile}.

		\item \textbf{Zakleszczenia i blokady.} Blokady realizowane są przez kolejkę żądań sortowaną po $(L, id)$ i mechanizm ACK. Master buduje graf oczekiwań (wait-for graph) i wykrywa cykle (DFS), a następnie wysyła \texttt{LockAbort} do ofiary o najniższym priorytecie w cyklu.

		\item \textbf{Streaming.} \texttt{backend/dfs/streaming/} zawiera binarny protokół transferu (nagłówek o stałej długości + stream danych) oraz zarządzanie sesjami uploadu. Inicjalizacja sesji odbywa się u mastera, a klient wysyła dane bezpośrednio do wybranych storage nodes.	
	\end{itemize}
	\subsubsection{Diagram Klas UML}
	Poniższy diagram przedstawia uproszczoną strukturę klas kluczowych komponentów systemu backendowego (Go).
	
	\begin{figure}[htbp]
		\centering
		\begin{tikzpicture}[font=\small]
			\node[draw, rectangle, inner sep=0pt] (Node) {
				\begin{tabular}{l}
				\textbf{Node} \\
				\hline
				+ ID: string \\
				+ Role: Role \\
				+ Peers: map[string]NodeInfo \\
				+ LamportClock: int64 \\
				\hline
				+ Start() \\
				+ HandleMessage(msg) \\
				+ RequestLock(resource) \\
				\end{tabular}
			};
			
			\node[draw, rectangle, inner sep=0pt, below=1.5cm of Node] (Bully) {
				\begin{tabular}{l}
				\textbf{BullyElector} \\
				\hline
				+ Priority: int \\
				\hline
				+ StartElection() \\
				+ HandleElectionMsg() \\
				\end{tabular}
			};
			
			\node[draw, rectangle, inner sep=0pt, right=2cm of Node] (Storage) {
				\begin{tabular}{l}
				\textbf{LocalStorage} \\
				\hline
				+ RootPath: string \\
				\hline
				+ SaveFile(id, data) \\
				+ GetFile(id) \\
				+ HasFile(id) \\
				\end{tabular}
			};
			
			\node[draw, rectangle, inner sep=0pt, below=1.5cm of Storage] (Stream) {
				\begin{tabular}{l}
				\textbf{StreamServer} \\
				\hline
				+ Port: int \\
				\hline
				+ Listen() \\
				+ HandleConnection() \\
				\end{tabular}
			};
			
			\draw[-Diamond] (Bully) -- (Node);
			\draw[-Diamond] (Storage) -- (Node);
			\draw[-Diamond] (Stream) -- (Node);
			
		\end{tikzpicture}
		\caption{Diagram klas głównych komponentów węzła RSP.}
	\end{figure}

	\subsubsection{Frontend (React + Vite)}
	Frontend znajduje się w \texttt{frontend/}.
	\begin{itemize}
		\item \texttt{src/pages/} -- widoki (np. logowanie, monitoring, węzły, pliki).
		\item \texttt{src/hooks/} -- logika komunikacji z API (np. pobieranie listy plików, upload/download).
		\item \texttt{src/components/} -- komponenty UI (lista plików, podgląd, upload, widok węzłów).
	\end{itemize}
	UI wykorzystuje API do operacji użytkownika, a sam transfer danych plików przebiega zgodnie z protokołem streaming (init u mastera + upload/download do storage).

	\subsubsection{Przykładowe fragmenty kodu}
	Poniżej przedstawiono fragmenty implementacji ilustrujące kluczowe mechanizmy rozproszone.

	\textbf{Warunek wejścia do sekcji krytycznej} (\texttt{node/node.go}) -- węzeł może wejść tylko gdy jest pierwszy w kolejce i zebrał wszystkie ACK:
	\begin{lstlisting}
func (n *Node) CanEnterCriticalSection(resourceID string) bool {
    // ... timeout handling ...
    first := n.lockQueue[resourceID][0]
    if first.NodeID != n.ID {
        return false  // nie jestesmy pierwsi w kolejce
    }
    expectedAcks := len(n.peers)
    receivedAcks := 0
    for _, acked := range n.pendingAcks[resourceID] {
        if acked { receivedAcks++ }
    }
    return receivedAcks >= expectedAcks
}
	\end{lstlisting}

	\textbf{Porównywanie żądań blokad} (\texttt{common/lamport.go}) -- sortowanie żądań po czasie Lamporta z tie-breakiem na ID węzła:
	\begin{lstlisting}
func CompareLockRequests(a, b *LockRequest) bool {
    if a.LogicalTime != b.LogicalTime {
        return a.LogicalTime < b.LogicalTime
    }
    return a.NodeID.String() < b.NodeID.String()
}
	\end{lstlisting}

	\textbf{Reakcja na elekcję Bully} (\texttt{election/bully.go}) -- węzeł o wyższym priorytecie odpowiada OK i sam startuje elekcję:
	\begin{lstlisting}
func (b *BullyElector) handleElection(ctx context.Context, msg Message) error {
    var nodeInfo common.NodeInfo
    json.Unmarshal(msg.Payload, &nodeInfo)

    if b.node.GetPriority() > nodeInfo.Priority {
        // Mamy wyzszy priorytet - odpowiadamy OK
        msgOk := Message{Type: MessageOK, From: b.node.GetID()}
        go b.node.SendMessage(nodeInfo.IP, nodeInfo.Port, msgOk)
        b.electionMutex.Lock()
        inProgress := b.electionInProgress
        b.electionMutex.Unlock()
        // Sami startujemy elekcje (moze wygramy)
        if !b.electionInProgress && b.node.GetRole() != RoleMaster {
            go b.StartElection(ctx)
        }
    }
    return nil
}
	\end{lstlisting}
	\subsection{Testy}
	Testy jednostkowe zaimplementowano w Go w katalogu \texttt{backend/dfs/} (pliki \texttt{\_test.go}). Pokrywają one głównie logikę algorytmiczną (bez uruchamiania pełnego klastra).

	\subsubsection{Uruchamianie}
	\begin{verbatim}
make test
	\end{verbatim}

	\subsubsection{Zakres}
	\begin{itemize}
		\item \texttt{backend/dfs/common/waitfor-graph\_test.go} -- testy detekcji cyklu w grafie oczekiwań (przypadki: graf pusty, pętla własna, cykle 2- i 3-węzłowe, graf rozłączny).
		\item \texttt{backend/dfs/hashing/hash\_test.go} -- testy consistent hashing (deterministyczność \texttt{HashKey}, węzły wirtualne, dodawanie/usuwanie węzłów, stabilność wyboru replik dla tego samego \texttt{FileID}).
		\item \texttt{backend/dfs/storage/local\_storage\_test.go} -- testy \texttt{LocalStorage} (\texttt{SaveFile/GetFile/DeleteFile}, idempotencja usuwania, zapis/odczyt \texttt{index.json}, deduplikacja: ta sama zawartość $\Rightarrow$ ten sam hash).
		\item \texttt{backend/dfs/node/node\_test.go} -- testy logiki węzła (kolejki/ACK blokad, integracja z grafem oczekiwań, wybór ofiary zakleszczenia, obsługa metadanych i replikacji).
	\end{itemize}

	\subsubsection{Wynik (stan bieżący repozytorium)}
	W aktualnym stanie repozytorium polecenie \texttt{make test} kończy się sukcesem (w tym pakiet \texttt{dfs/node}).
	\begin{verbatim}
ok   dfs-backend/dfs/common   (tests passed)
ok   dfs-backend/dfs/hashing  (tests passed)
ok   dfs-backend/dfs/storage  (tests passed)
ok   dfs-backend/dfs/node     (tests passed)
	\end{verbatim}

	\subsubsection{Testy integracyjne/scenariusze (\texttt{backend/cmd/test-*})}
	Oprócz testów jednostkowych w repozytorium znajdują się scenariusze integracyjne, które uruchamiają mini-klaster wielu węzłów i weryfikują poprawność działania mechanizmów rozproszonych w praktyce. Scenariusze te wymagają ręcznego uruchomienia i analizy logów -- nie zostały zintegrowane z automatycznym \texttt{make test}.
	
	W projekcie wykonano również analizę pod kątem \textbf{testów mutacyjnych}. Ze względu na specyfikę algorytmów rozproszonych (gdzie błędy często wynikają z kolejności zdarzeń, a nie błędów operacji arytmetycznych), klasyczne testy mutacyjne zastąpiono \textbf{testami odpornościowymi (chaos engineering)}. Scenariusze \texttt{test-bully} oraz \texttt{test-sync} symulują losowe awarie węzłów (mutacja stanu klastra), weryfikując czy system potrafi powrócić do stabilnego stanu (self-healing). Jest to podejście bardziej adekwatne dla systemów rozproszonych niż prosta mutacja kodu źródłowego.

	Uruchamianie (w katalogu \texttt{backend/}):
	\begin{verbatim}
go run ./cmd/test-message
go run ./cmd/test-bully
go run ./cmd/test-lock
go run ./cmd/test-sync
	\end{verbatim}
	Opis scenariuszy:
	\begin{itemize}
		\item \texttt{test-message} -- 2 węzły: weryfikacja discovery i wymiany heartbeat.
		\item \texttt{test-bully} -- 3 węzły: symulacja awarii lidera i przebieg elekcji Bully.
		\item \texttt{test-lock} -- 5 węzłów: kolejkowanie żądań blokad i wejścia do sekcji krytycznej.
		\item \texttt{test-sync} -- 4 węzły: awaria mastera i synchronizacja metadanych po elekcji.
	\end{itemize}
	
	\subsection{Eksperymenty}
	Poniżej przedstawiono propozycję eksperymentów, które pozwalają ilościowo opisać zachowanie systemu (czas reakcji, narzut algorytmów, przepustowość) oraz porównać wybrane parametry konfiguracji.

	W ramach repozytorium dostępne są gotowe komendy eksperymentalne (\texttt{backend/cmd/exp-*}), które zapisują wyniki do plików CSV w \texttt{backend/experiments/*.csv}. Na ich podstawie generowane są wykresy (\texttt{experiments/plot\_csvs.py} \(\Rightarrow\) \texttt{experiments/plots/*.png}).

	\subsubsection{Metodyka pomiaru}
	\begin{itemize}
		\item Każdy pomiar wykonywać $n\ge 10$ razy; raportować średnią, medianę oraz odchylenie standardowe.
		\item Mierzone czasy: w ms; przepustowość: $\mathrm{MB}/\mathrm{s}$; nierównomierność rozkładu: min/avg/max oraz $\sigma$.
		\item Warunki: stała liczba węzłów, stała konfiguracja sieci, brak innych obciążeń w tle.
	\end{itemize}

	\subsubsection{Eksperyment 1: czas przejęcia lidera (Bully)}
	\textbf{Cel:} zmierzyć czas od awarii mastera do ponownej dostępności usługi (wybór nowego lidera i stabilizacja).
	\begin{itemize}
		\item Narzędzie: \texttt{go run ./cmd/exp-bully}.
		\item Metryki (CSV: \texttt{bully.csv}):
		\begin{itemize}
			\item \texttt{failover\_ms} -- czas od „zabicia" mastera do momentu, gdy klaster znów odpowiada.
			\item \texttt{new\_master\_priority} -- weryfikacja poprawności Bully (czy wygrał węzeł o najwyższym priorytecie).
		\end{itemize}
	\end{itemize}
	\textbf{Wyniki (10 przebiegów, 3 węzły):}
	\begin{center}
	\begin{tabular}{|l|c|}
	\hline
	Metryka & Wartość \\
	\hline
	Średnia \texttt{failover\_ms} & 15659 ms \\
	Mediana & 16020 ms \\
	Min / Max & 14196 / 16032 ms \\
	\hline
	\end{tabular}
	\end{center}
	We wszystkich przebiegach nowym masterem został węzeł o priorytecie 5 (najwyższym wśród żywych) -- algorytm Bully działa poprawnie. Czas failover to głównie timeout heartbeat (15s) + czas elekcji.
	
	\begin{figure}[htbp]
		\centering
		\includegraphics[width=0.7\textwidth]{experiments/plots/bully_failover_hist.png}
		\caption{Rozkład czasu failover (Bully) -- histogram z 10 przebiegów.}
	\end{figure}

	\subsubsection{Eksperyment 2: narzut mechanizmu blokad (Lamport+ACK)}
	\textbf{Cel:} ocenić opóźnienie wejścia do sekcji krytycznej przy rosnącej konkurencji.
	\begin{itemize}
		\item Narzędzie: \texttt{go run ./cmd/exp-lock}.
		\item Metryki (CSV: \texttt{lock.csv}):
		\begin{itemize}
			\item \texttt{wait\_ms} -- czas od zgłoszenia żądania blokady do wejścia do sekcji krytycznej.
			\item \texttt{order} -- kolejność uzyskania blokady (analiza fairness).
		\end{itemize}
		\item Scenariusz: 5 węzłów jednocześnie żąda blokady na ten sam zasób.
	\end{itemize}
	\textbf{Wyniki (10 przebiegów, 5 węzłów):}
	\begin{center}
	\begin{tabular}{|c|c|}
	\hline
	Kolejność (order) & Średni \texttt{wait\_ms} \\
	\hline
	1 (pierwszy) & $\sim$12 ms \\
	2 & $\sim$223 ms \\
	3 & $\sim$433 ms \\
	4 & $\sim$645 ms \\
	5 (ostatni) & $\sim$856 ms \\
	\hline
	\end{tabular}
	\end{center}
	Opóźnienie rośnie liniowo z kolejnością -- każdy węzeł czeka na zakończenie operacji poprzedników. Sekcja krytyczna trwa $\sim$200 ms, co odpowiada przyrostowi między kolejnymi pozycjami.
	
	\begin{figure}[htbp]
		\centering
		\includegraphics[width=0.7\textwidth]{experiments/plots/lock_wait_boxplot_by_order.png}
		\caption{Czas oczekiwania na blokadę w zależności od kolejności (boxplot).}
	\end{figure}

	\subsubsection{Eksperyment 3: detekcja zakleszczeń (wait-for graph)}
	\textbf{Cel:} zmierzyć czas przerwania zakleszczenia (czas „powrotu do pracy" po wystąpieniu cyklu oczekiwań).
	\begin{itemize}
		\item Narzędzie: \texttt{go run ./cmd/exp-deadlock}.
		\item Scenariusz: 2 węzły tworzą cykl $A\rightarrow B\rightarrow A$ (każdy trzyma lock i żąda locka drugiego).
		\item Metryki (CSV: \texttt{deadlock.csv}):
		\begin{itemize}
			\item \texttt{resolve\_ms} -- czas od powstania zakleszczenia do jego przerwania przez mastera.
		\end{itemize}
	\end{itemize}
	\textbf{Wyniki (10 przebiegów):}
	\begin{center}
	\begin{tabular}{|l|c|}
	\hline
	Metryka & Wartość \\
	\hline
	Średnia \texttt{resolve\_ms} & 9317 ms \\
	Mediana & 9317 ms \\
	Min / Max & 9311 / 9322 ms \\
	$\sigma$ & 3.5 ms \\
	\hline
	\end{tabular}
	\end{center}
	Czas rozwiązania zakleszczenia jest bardzo stabilny ($\sigma \approx 3.5$ ms) i wynosi $\sim$9.3s. Wynika to z interwału sprawdzania grafu przez mastera (10s) -- zakleszczenie zostaje wykryte przy najbliższym cyklu analizy.
	
	\begin{figure}[htbp]
		\centering
		\includegraphics[width=0.7\textwidth]{experiments/plots/deadlock_resolve_hist.png}
		\caption{Rozkład czasu rozwiązania zakleszczenia -- histogram.}
	\end{figure}

	\subsubsection{Eksperyment 4: przepustowość upload/download (streaming)}
	\textbf{Cel:} ocenić przepustowość protokołu streaming.
	\begin{itemize}
		\item Narzędzie: \texttt{go run ./cmd/exp-streaming}.
		\item Scenariusz: upload i download plików 16 MB.
		\item Metryki (CSV: \texttt{streaming.csv}):
		\begin{itemize}
			\item \texttt{upload\_ms}, \texttt{upload\_mbps} -- czas i przepustowość uploadu.
			\item \texttt{download\_ms}, \texttt{download\_mbps} -- czas i przepustowość downloadu.
		\end{itemize}
	\end{itemize}
	\textbf{Wyniki (10 przebiegów, plik 16 MB):}
	\begin{center}
	\begin{tabular}{|l|c|c|}
	\hline
	Operacja & Średni czas & Średnia przepustowość \\
	\hline
	Upload & 345 ms & 392 MB/s \\
	Download & 55 ms & 1110 MB/s* \\
	\hline
	\end{tabular}
	\end{center}
	*Część downloadów zakończyła się w $<$1 ms (cache OS/dysku), co zawyża średnią przepustowość. Upload jest wolniejszy, ponieważ dane są replikowane równolegle na 3 węzły storage.
	
	\begin{figure}[htbp]
		\centering
		\includegraphics[width=0.7\textwidth]{experiments/plots/streaming_throughput.png}
		\caption{Przepustowość upload/download w kolejnych przebiegach.}
	\end{figure}

\end{document}